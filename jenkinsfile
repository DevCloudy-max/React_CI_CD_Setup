pipeline {
    agent any
    tools { nodejs "NodeJS" } // Use installed NodeJS version
    stages {
        stage('build') {
            steps {
                sh '''
                    ls -l
                    node -v
                    npm -v
                    npm install
                    npm run build
                    ls -l
                '''
            }
        }
    }
}
